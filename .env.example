# Shared 5th Avenue backend (5th-internal-back) — the portal reads campaign and
# analytics data from it, and the public "Start a project" / creator-application
# forms POST their submissions to it. Defaults to http://localhost:4000 if unset.
VITE_API_URL=http://localhost:4000
